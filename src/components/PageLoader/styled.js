import styled from '@emotion/styled';

const Styled = {
  Wrapper: styled.div`
    height: 100%;
    width: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
  `
};

export default Styled;
